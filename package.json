{
  "name": "aulagulp",
  "version": "1.0.0",
  "description": "AulaGulp build",
  "main": "src/index.html",
  "scripts": {
    "build": "yarn gulp build && yarn gulp server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hog",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.26.12",
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^7.0.1",
    "gulp-concat": "^2.6.1",
    "gulp-minify": "^3.1.0",
    "gulp-minify-css": "^1.2.4",
    "gulp-rename": "^2.0.0",
    "gulp-uglify": "^3.0.2"
  }
}
